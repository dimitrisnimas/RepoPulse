import{resolveTheme}from"@/config/themes";import{svgDocument}from"@/server/cards/card-renderer";import{escapeXml,formatNumber,safeAvatarUrl,truncateText}from"@/server/cards/card-utils";import{websiteDomain}from"./profile.mapper";import type{ProfileCardData,ProfileOptions}from"./profile.types";
export function renderProfileCard(data:ProfileCardData,o:ProfileOptions){const{theme}=resolveTheme(o.theme),w=o.width,avatar=o.showAvatar?safeAvatarUrl(data.avatarUrl):null,x=avatar?104:24;const fields=[o.showCompany&&data.company?`⌂ ${truncateText(data.company,22)}`:null,o.showLocation&&data.location?`⌖ ${truncateText(data.location,22)}`:null,o.showWebsite&&websiteDomain(data.websiteUrl)?`↗ ${websiteDomain(data.websiteUrl)}`:null].filter(Boolean);const h=238+(o.showLanguages?42:0)+(o.showStatus&&data.status?32:0);let c=`${avatar?`<defs><clipPath id="pavatar"><circle cx="58" cy="64" r="34"/></clipPath></defs><image href="${escapeXml(avatar)}" x="24" y="30" width="68" height="68" clip-path="url(#pavatar)"/>`:""}<text x="${x}" y="45" class="title" font-size="19">${escapeXml(truncateText(data.displayName??data.username,30))}</text><text x="${x}" y="66" class="muted" font-size="11">@${escapeXml(data.username)}</text>${o.showBio?`<text x="${x}" y="89" class="text" font-size="10">${escapeXml(truncateText(data.bio??"Public GitHub developer",Math.max(28,Math.floor((w-x)/6.5))))}</text>`:""}${o.showStatus&&data.status?`<rect x="24" y="111" width="${w-48}" height="25" rx="8" fill="${theme.progressBackground}"/><text x="36" y="128" class="text" font-size="9">${escapeXml(`${data.status.emoji??""} ${truncateText(data.status.message,55)}`.trim())}</text>`:""}`;const base=o.showStatus&&data.status?158:125;c+=fields.map((f,i)=>`<text x="${24+i*(w-48)/Math.max(1,fields.length)}" y="${base}" class="muted" font-size="9">${escapeXml(String(f))}</text>`).join("");const metrics=[o.showFollowers?["Followers",data.followers]:null,o.showRepositories?["Repositories",data.publicRepositories]:null,o.showContributions?["Contributions",data.totalContributions]:null].filter(Boolean)as Array<[string,number]>;c+=metrics.map(([label,value],i)=>{const cx=24+i*(w-48)/metrics.length;return`<text x="${cx}" y="${base+37}" class="title" font-size="17">${formatNumber(value,o.locale)}</text><text x="${cx}" y="${base+53}" class="muted" font-size="9">${label}</text>`}).join("");if(o.showLanguages)c+=data.topLanguages.slice(0,4).map((l,i)=>`<circle cx="${24+i*(w-48)/4}" cy="${base+85}" r="4" fill="${l.color}"/><text x="${34+i*(w-48)/4}" y="${base+89}" class="muted" font-size="9">${escapeXml(truncateText(l.name,11))}</text>`).join("");return svgDocument({width:w,height:h,theme,hideBorder:o.hideBorder,title:`${data.username}'s developer profile`,description:data.bio??`Public GitHub profile for ${data.username}`,content:c})}
