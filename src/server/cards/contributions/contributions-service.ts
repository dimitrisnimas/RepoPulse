import{getCachedSvg,setCachedSvg}from"@/server/cache/cache-client";import{getContributionCalendar}from"@/server/github/github-service";import{mapContributionsData}from"./contributions.mapper";import type{ContributionsCardData}from"./contributions.types";
export async function getNormalizedContributions(username:string,year:number):Promise<ContributionsCardData>{const key=`repopulse:data:contributions:v1:${username.toLowerCase()}:${year}`;const cached=await getCachedSvg(key);if(cached?.fresh)return JSON.parse(cached.value)as ContributionsCardData;try{const data=mapContributionsData(await getContributionCalendar(username,year),year);await setCachedSvg(key,JSON.stringify(data),3600);return data}catch(error){if(cached)return JSON.parse(cached.value)as ContributionsCardData;throw error}}
