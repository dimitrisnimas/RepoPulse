import { describe, expect, it } from "vitest";
import { renderPrivateActivityCard } from "./private-activity-card";
import { selectedPrivateRepositories } from "./private-activity-service";
describe("private activity card",()=>{
  it("accepts only Dimitris' explicitly selected repositories",()=>{expect(selectedPrivateRepositories("dimitrisnimas/one, two\n'three';dimitrisnimas/one")).toEqual(["one","two","three"]);for(const value of["someone/repo","dimitrisnimas/../secret","dimitrisnimas/repo/extra","https://github.com/dimitrisnimas/repo"])expect(()=>selectedPrivateRepositories(value)).toThrow()});
  it("renders aggregate metadata and escapes private repository names",()=>{const svg=renderPrivateActivityCard({username:"dimitrisnimas",about:"Building private products & reliable APIs.",repositories:[{name:"private<app>",language:"TypeScript",languageColor:"#3178c6",totalCommits:140,lastCommitAt:"2026-08-01T12:00:00Z"}],unavailable:0,generatedAt:"2026-08-02T00:00:00Z"},{theme:"dark",width:680,hideBorder:false,locale:"en"});expect(svg).toContain("private&lt;app&gt;");expect(svg).toContain("Building private products &amp; reliable APIs.");expect(svg).toContain("140 commits");expect(svg).toContain("repositories");expect(svg).toContain("Built with");expect(svg).toContain("https://repopulse.kubik.gr/RepoPulse.png");expect(svg).not.toContain("Metadata only");expect(svg).not.toContain("more private repositories");expect(svg).not.toContain("private<app>");expect(svg).not.toContain("<script")});
  it("reports partial repository failures without leaking their names",()=>{const svg=renderPrivateActivityCard({username:"dimitrisnimas",about:"Private work.",repositories:[],unavailable:2,generatedAt:"2026-08-02T00:00:00Z"},{theme:"midnight",width:500,hideBorder:true,locale:"en"});expect(svg).toContain("2 temporarily unavailable")});
});
