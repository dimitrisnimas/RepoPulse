import{env}from"@/config/env";export type CircuitState="closed"|"open"|"half-open";interface State{failures:number;openedAt:number|null;testing:boolean}const g=globalThis as typeof globalThis&{__rpCircuit?:State};const s=g.__rpCircuit??={failures:0,openedAt:null,testing:false};g.__rpCircuit=s;
export function getCircuitState(now=Date.now()):CircuitState{if(s.openedAt===null)return"closed";return now-s.openedAt>=env.REPOPULSE_CIRCUIT_COOLDOWN_SECONDS*1000?"half-open":"open"}export function allowCircuitRequest(now=Date.now()){const state=getCircuitState(now);if(state==="closed")return true;if(state==="open"||s.testing)return false;s.testing=true;return true}export function circuitSuccess(){s.failures=0;s.openedAt=null;s.testing=false}export function circuitFailure(now=Date.now()){s.testing=false;s.failures++;if(s.failures>=env.REPOPULSE_CIRCUIT_FAILURE_THRESHOLD)s.openedAt=now}export function resetCircuit(){s.failures=0;s.openedAt=null;s.testing=false}
