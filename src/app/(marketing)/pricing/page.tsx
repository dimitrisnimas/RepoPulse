import type { Metadata } from "next";
import { Check } from "lucide-react";
import { Badge, Card } from "@/components/ui/primitives";
import { Button } from "@/components/ui/button";
export const metadata: Metadata = { title: "Pricing", description: "Simple RepoPulse plans for every kind of GitHub profile." };
const plans = [{ name:"Free", price:"€0", copy:"Everything you need for a standout profile.", features:["Public GitHub cards","Core themes","Community support"], action:"Open Playground", href:"/playground" },{ name:"Pro", price:"Soon", copy:"Private insights and deeper customization.", features:["Private contribution stats","Custom themes","Higher rate limits"], soon:true },{ name:"Business", price:"Custom", copy:"Reliable metrics for teams and organizations.", features:["Organization dashboards","Priority support","Custom limits"], soon:true }];
export default function Pricing() { return <div className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="mx-auto max-w-2xl text-center"><Badge>Simple pricing</Badge><h1 className="mt-6 text-4xl font-semibold tracking-tight sm:text-6xl">Start free. Scale when you need to.</h1><p className="mt-5 text-zinc-500">No payment details. No hidden complexity.</p></div><div className="mt-16 grid gap-4 lg:grid-cols-3">{plans.map((plan,index) => <Card key={plan.name} className={`p-7 ${index === 0 ? "border-violet-400/30 bg-violet-400/[.035]" : ""}`}><div className="flex items-center justify-between"><h2 className="text-lg font-medium">{plan.name}</h2>{plan.soon && <Badge>Coming soon</Badge>}</div><p className="mt-7 text-4xl font-semibold">{plan.price}</p><p className="mt-4 min-h-12 text-sm leading-6 text-zinc-500">{plan.copy}</p><div className="my-7 h-px bg-white/[.08]" /><ul className="space-y-4">{plan.features.map((item) => <li key={item} className="flex items-center gap-3 text-sm text-zinc-400"><Check size={15} className="text-violet-300" />{item}</li>)}</ul><Button href={plan.href} variant={index ? "secondary" : "primary"} disabled={plan.soon} className="mt-9 w-full">{plan.action ?? "Join waitlist"}</Button></Card>)}</div></div>; }
