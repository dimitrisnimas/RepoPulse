import{describe,expect,it}from"vitest";import{GET as health}from"./health/route";import{GET as status}from"./status/route";import{GET as readiness}from"./readiness/route";import{GET as refresh}from"./internal/refresh/route";
describe("diagnostics",()=>{it("returns safe health and status payloads",async()=>{const h=await health().json(),s=await status().json();expect(h).toMatchObject({status:"ok",service:"repopulse"});expect(s.cards).toHaveLength(8);expect(s.cards).toContain("private-activity");expect(JSON.stringify(s)).not.toMatch(/token|redis.*url/i)});it("reports readiness without exposing secrets",async()=>{const response=await readiness();const body=await response.json();expect(body.dependencies).toBeDefined();expect(JSON.stringify(body)).not.toContain("Bearer")});it("protects cron refresh",async()=>expect((await refresh(new Request("http://localhost/api/internal/refresh"))).status).toBe(401))});
