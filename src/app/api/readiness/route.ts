import{NextResponse}from"next/server";import{env}from"@/config/env";import{cacheCommand}from"@/server/cache/cache-client";import{getCircuitState}from"@/server/reliability/circuit-breaker";
export async function GET(){let cache:"available"|"optional"|"unavailable"=env.UPSTASH_REDIS_REST_URL?"unavailable":"optional";if(env.UPSTASH_REDIS_REST_URL)try{await cacheCommand(["PING"]);cache="available"}catch{}const github=env.GITHUB_TOKEN?"configured":"unavailable",circuit=getCircuitState(),ready=github==="configured"&&circuit!=="open";return NextResponse.json({status:ready?"ready":"degraded",dependencies:{github,cache},circuit,version:env.APP_VERSION,timestamp:new Date().toISOString()},{status:ready?200:503})}
