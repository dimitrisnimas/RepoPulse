import{cardCacheKey}from"@/server/cache/cache-keys";import{handleCardRoute}from"@/server/cards/card-route";import{getNormalizedContributions}from"@/server/cards/contributions/contributions-service";import{renderStreakCard}from"@/server/cards/streak/streak-card";import{mapStreakData}from"@/server/cards/streak/streak.mapper";import{parseStreakQuery,streakQuerySchema}from"@/server/cards/streak/streak.schema";
export const runtime="nodejs";type Input=ReturnType<typeof streakQuerySchema.parse>;
export function GET(request:Request){return handleCardRoute<Input>({request,parse:parseStreakQuery,freshSeconds:3600,width:(i)=>i.width,theme:(i)=>i.theme,cacheKey:(i)=>cardCacheKey("streak","v1",{...i,username:i.username.toLowerCase()}),generate:async(i)=>{const data=await getNormalizedContributions(i.username,i.year);const reference=i.year===new Date().getUTCFullYear()?new Date():new Date(Date.UTC(i.year,11,31));return renderStreakCard(mapStreakData(data,reference),{username:i.username,theme:i.theme,width:i.width,year:i.year,hideBorder:i.hide_border,showRing:i.show_ring,locale:i.locale});}});}
