import Link from "next/link";
import { Activity, BarChart3, CreditCard, Github, LayoutDashboard, LogOut, Palette, Settings } from "lucide-react";
const nav = [[LayoutDashboard,"Overview"],[CreditCard,"Generated cards"],[BarChart3,"API usage"],[Github,"GitHub connection"],[Palette,"Themes"],[Settings,"Settings"]] as const;
export default function DashboardLayout({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-[#080809]"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-white/[.08] p-5 lg:block"><Link href="/" className="flex items-center gap-2 font-semibold"><Activity size={18} className="text-violet-300" />RepoPulse</Link><p className="mt-9 px-3 text-[10px] font-medium uppercase tracking-wider text-zinc-700">Workspace</p><nav className="mt-3 space-y-1">{nav.map(([Icon,label],index) => <a key={label} href="#" className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${index === 0 ? "bg-white/[.06] text-white" : "text-zinc-500 hover:text-white"}`}><Icon size={16} />{label}</a>)}</nav><button className="absolute bottom-5 left-5 right-5 flex items-center gap-3 rounded-lg px-3 py-2 text-sm text-zinc-600 hover:text-white"><LogOut size={15} />Sign out</button></aside><div className="lg:pl-64"><header className="flex h-16 items-center border-b border-white/[.08] px-5 lg:px-8"><p className="text-sm text-zinc-500">Personal workspace <span className="mx-2 text-zinc-800">/</span> <span className="text-white">Overview</span></p><div className="ml-auto flex items-center gap-3"><span className="hidden text-xs text-zinc-600 sm:block">Preview mode</span><span className="grid size-8 place-items-center rounded-full bg-violet-500/20 text-xs text-violet-200">K</span></div></header>{children}</div></div>; }
