import { GitFork, Star, Users } from "lucide-react";

export function StatsCard({ compact = false }: { compact?: boolean }) {
  return <div className={`relative overflow-hidden rounded-2xl border border-white/10 bg-[#0d0d10] p-6 shadow-2xl shadow-violet-950/20 ${compact ? "max-w-lg" : ""}`}><div className="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-violet-400/70 to-transparent" /><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-full bg-gradient-to-br from-violet-400 to-indigo-700 font-semibold">K</div><div><p className="font-semibold">kubikgr</p><p className="text-xs text-zinc-500">Building dependable software</p></div><span className="ml-auto rounded-full border border-emerald-400/20 bg-emerald-400/10 px-2 py-1 text-[10px] text-emerald-300">LIVE</span></div><div className="mt-7 grid grid-cols-3 gap-3">{[[Star, "Stars", "2.4k"], [GitFork, "Commits", "8,921"], [Users, "Followers", "684"]].map(([Icon, label, value]) => { const I = Icon as typeof Star; return <div key={String(label)} className="rounded-xl border border-white/[0.07] bg-white/[0.025] p-3"><I size={14} className="mb-3 text-violet-300" /><p className="text-lg font-semibold">{value as string}</p><p className="text-[11px] text-zinc-500">{label as string}</p></div>; })}</div><div className="mt-5 flex h-14 items-end gap-1">{[28,44,34,70,46,58,86,52,74,62,92,68,78,95].map((height, index) => <span key={index} className="flex-1 rounded-t-sm bg-gradient-to-t from-violet-700/40 to-violet-400/80" style={{ height: `${height}%` }} />)}</div><p className="mt-3 text-[10px] text-zinc-600">Contribution activity · Last 14 weeks</p></div>;
}
